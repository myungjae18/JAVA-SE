class SkillE extends GameObject{
	constructor(type,container,x,y,width,height,velX,velY,bg,src,targetX,targetY){
		super(type,container,x,y,width,height,velX,velY,bg,src,targetX,targetY);
		/*var obj=objectManager.objectArray[3];
		addEventListener("keyup", ()=>{
			if(key==69){
				objectManager.removeObject(obj);
				objectManager.removeObject(this);
			}
		});*/
	}
}